{
  "name": "Roadworks",
  "author": "MAPC Digital Services Working Group",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/MAPC/roadworks.git"
  },
  "scripts": {
    "postinstall": "cd ./client && npm install",
    "test": "npm run build:test && npm run lint && rspec",
    "lint": "cd client && npm run lint",
    "build:clean": "rm -rf public/webpack || true",
    "build:production:client": "(cd client && npm run --silent build:production:client)",
    "build:production:server": "(cd client && npm run --silent build:production:server)",
    "build:client": "(cd client && npm run --silent build:client",
    "build:server": "(cd client && npm run --silent build:server)",
    "build:dev:client": "(cd client && npm run --silent build:dev:client)",
    "build:dev:server": "(cd client && npm run --silent build:dev:server)",
    "hot-assets": "(cd client && npm run hot-assets)"
  },
  "dependencies": {
    "@rails/webpacker": "^3.4.3",
    "babel-preset-react": "^6.24.1",
    "prop-types": "^15.6.1",
    "react": "^16.3.0",
    "react-dom": "^16.3.0",
    "webpack": "^4.4.1"
  },
  "devDependencies": {
    "webpack-cli": "^2.0.13",
    "webpack-dev-server": "^3.1.1"
  }
}
